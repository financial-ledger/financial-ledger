import Image from "next/image";
import { css } from "styled-system/css";

export default function Home() {
  return (
    <div
      className={css({
        color: "red",
      })}
    >
      test
    </div>
  );
}
